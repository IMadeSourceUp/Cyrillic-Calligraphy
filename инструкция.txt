Установить Python 3.8+ (nuff said)
Вызвать в папке терминал/Powershell и запустить команду pip install -r ./requirements.txt
запустить команду python main.py
Графического интерфейса на данный момент нет, поэтому догадки нейросети выводятся в консоль, а машинное зрение в отдельное окно.































знаю-знаю, продукт сырой. никогда до этого не работал с нейронками